package com.hrachovblackblaze.blackblazestreamingmicroservice.exception;
/*
import jakarta.servlet.http.HttpServletRequest;
import org.apache.catalina.connector.ClientAbortException;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.web.bind.annotation.ControllerAdvice;
import org.springframework.web.bind.annotation.ExceptionHandler;

@ControllerAdvice
public class GlobalExceptionHandler {
    private static final Logger log = LoggerFactory.getLogger(GlobalExceptionHandler.class);

    @ExceptionHandler(ClientAbortException.class)
    public void handleClientAbort(HttpServletRequest req, ClientAbortException ex) {
        // Клиент оборвал соединение — логируем коротко и не поднимаем исключение
        log.info("Client aborted connection: {} {}", req.getMethod(), req.getRequestURL());
    }
}

 */